# Склады

2 действия в домене `warehouses`.

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `warehouse-list` | Список складов продавца | — |
| `delivery-methods` | Способы доставки | `filter_dict` (dict), `limit` (int, 50), `offset` (int, 0) |
