# Штрихкоды

2 действия в домене `barcodes`.

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `barcode-generate` | Сгенерировать штрихкоды | `product_id` (list[int]) * |
| `barcode-add` | Привязать штрихкод к товару | `barcodes` (list[dict]) * |

\* — обязательный параметр
